rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Enable live synchronization for all TechFix collections across Admin, Technician, and Customer
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
